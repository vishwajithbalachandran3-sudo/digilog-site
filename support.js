document.querySelector('#tracking-form').addEventListener('submit',event=>{event.preventDefault();const order=event.currentTarget.querySelector('input').value.trim().toUpperCase();document.querySelector('#tracking-result').textContent=`For the latest status of ${order}, please email digilogmicro@info.com. Include the email address used for the order.`});
document.querySelectorAll('.faq-item button').forEach(button=>button.addEventListener('click',()=>{const item=button.parentElement;item.classList.toggle('open');button.querySelector('span').textContent=item.classList.contains('open')?'−':'+'}));
